Visitor visits CCM{
    CCM checks whether visitor is authed{
        if authed{
            check if visitor is admin{
                check if app is authed{
                    if admin{
                        call authenticator function
                        authenticator returns a consent url, DS calls the callback url
                        CallbackUrl Shows the success page and Makes a change in the database that 
                        triggers the get token function.
                    }else{
                        Tell them the app needs to be authenticated. Ask your admin to authenticate with docusign
                    }
                }
                }
        }else{
            Prompt User to authenticate and redirect to home('/')
        }
    }
}